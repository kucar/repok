HP switches max session warning appears when limit of max open session count has been reached.
This code sends an snmp set command to kick all connected users out of the switch and lets the new 
user to ssh/telnet to the desired switch.